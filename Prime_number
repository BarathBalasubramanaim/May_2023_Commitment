#include<stdio.h>
int main()
{
char 
printf("Give me a number:");
scnaf("%d",&givennumber);
result=prime(&givennumber);
	if(esult==1)
	{
	printf("Prime number");
	}
	
	else
	{
	printf("Not Prime number");
	}
}

char prime(int* givennumber)
{
char diviser=2,prime=1,check1;
while(diviser<*givennumber)
{
check1=*givennumber%diviser;

	if(check1>0)
	{
	prime=0;
	break;
	}
	
	else
	{
	diviser++;
	}
}
return(prime);
}
